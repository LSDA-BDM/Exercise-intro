### Steps for solving exercises and assignments

1. Once you have a fork of the repository (a copy repository) you can open a *codespace* by pressing the button **<> Code**, then click on **Codespaces**, then **Create codespace on main**.
2. To practice updating the repository, navigate to this README.md file and make a small change (for example add some text).   
3. Commit and push the changes.
    1. For this, navigate to *Source Control* which will show you which files have changes (in this case only the README.md file).
    2. Choose the file you would like to commit and click the + button next to the filename; add a descriptive commit message and click *Commit*. You will then see that the changes are staged to commit.
    3. Press *Sync Changes* which will push your updates to the repository. After this you and the teachers will see the changes you made to the repo.
5. Now you can start working on the exercise. Navigate to `pandas_exercises.ipynb`. The dependencies needed to run the project will be installed in a local environment. For this exercise we only need the `pandas` library.
6. The first task is to import pandas `import pandas as pd`. Add the code and click run.
7. At the top of the notebook, under *Select Kernel* you can click *Install/Enable suggested extentions Python + Jupyter*
8. When you are prompted again to select a kernel choose *Python Environments*.
9. Then select the recommended python environment.
10. Now the import should work and you can continue with the rest of the exercise.
11. When you are done don't forget to update the repository.

### Resources
Check the [Git intro repository](https://github.com/LSDA-BDM/Git-intro) for details on the different aspects of git.
